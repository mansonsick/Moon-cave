# 互動式兒童冒險繪本開發 SOP

版本：v1.0  
基準專案：`Moon-cave`（《月光洞的祕密》）  
主要平台：10–11 吋平板瀏覽器  
主要受眾：閱讀能力尚在建立中的低年級兒童

---

## 0. 核心原則

這套作品不是「把故事文字放到網頁上」，而是把 **圖片、短句、選擇、挑戰、道具、隱藏探索** 組合成一個小型互動冒險。

固定原則：

1. **圖片先講故事，文字只補足圖片無法說清楚的部分。**
2. 每一幕只處理一個主要事件，避免同頁資訊過多。
3. 一般選項最多 2 個，直接放在圖片附近，使用淡淡呼吸光提示可互動。
4. 隱藏選項不發光、不做醒目按鈕，讓孩子從文字或圖片自己發現。
5. 不設計 Game Over。選錯仍能繼續，只是可能進普通結局。
6. 道具數量以 3 件左右為上限，避免狀態過多。
7. 每件重要道具原則上搭配一個小挑戰，成功後才取得。
8. 挑戰失敗不懲罰，提供「重新挑戰」。
9. 重要提示與關鍵道具必須在圖片中看得見，而且位置、動作、因果要與文字一致。
10. 所有互動都要能用手指完成，不依賴滑鼠 hover。

---

## 1. 新故事立項時先決定的 8 件事

每次開始新故事，先填完以下內容，不直接進入寫程式：

### 1.1 基本資料

- 故事名稱：
- 主角名稱：
- 主角固定外觀：
- 陪伴角色：
- 年齡層：
- 主要設備：平板 / 手機 / 電腦
- 直向或橫向：預設直向，可支援橫向
- 恐怖程度：無 / 微恐怖 / 中等但不血腥

### 1.2 教育／行為主題

每本最多 1–2 個核心概念，例如：

- 不以外表判斷善惡
- 先觀察再行動
- 貪心與真正價值
- 勇氣不是魯莽
- 幫助別人會得到意想不到的回報

不要每一幕都塞教訓，讓故事本身呈現結果。

### 1.3 道具

預設最多 3 件：

| 道具 | 取得事件 | 挑戰 | 後續用途 |
|---|---|---|---|
| 道具 A |  |  |  |
| 道具 B |  |  |  |
| 道具 C |  |  |  |

### 1.4 結局

預設使用：

- **普通結局**：沒蒐集完整道具仍可完成故事。
- **隱藏／祕密結局**：必須仔細探索或蒐集完整道具，自然觸發，不顯示「祕密結局」按鈕。

### 1.5 挑戰

優先從標準挑戰庫選，不要每次重新發明互動機制。

### 1.6 隱藏探索

每本最多 1 個主要隱藏探索入口，避免孩子完全找不到主線。

### 1.7 圖片風格

先決定一張「角色＋世界觀基準圖」，後續所有圖片都以它為 anchor，確保主角外觀一致。

### 1.8 文字密度

每幕建議：

- 2–6 個短句
- 每句盡量不超過 12–16 個中文字
- 不用長段落
- 對話可提高趣味，但不要一次出現太多角色

---

## 2. 故事節點標準格式

每一幕都用以下格式設計，確認後才產圖／寫程式。

```md
## Scene ID：stone_path
畫面名稱：古老石階

### 故事文字
阿通沿著石階往上走。
前面有倒掉的古牆。
路旁還放著一面舊盾牌。

### 圖片必要元素
- 主角阿通
- 古老石階
- 倒塌古牆
- 守衛盾牌必須清楚可見
- 盾牌附近不可被文字蓋住

### 一般互動
- 點盾牌 → `shieldChallenge()`
- 繼續前進 → 下一幕

### 隱藏互動
無

### 狀態變化
挑戰成功：`shield = true`

### 下一幕
完成或略過 → `skeleton_room`
```

這個格式是新故事最重要的交接文件。

---

## 3. 圖片製作標準

### 3.1 基本規格

- A4／平板直向構圖比例：約 `1055 × 1491` 或同等比例。
- 圖片本身 **不要放文字**。
- 重要物件不要貼邊，預留平板不同尺寸裁切安全區。
- 重要互動物件至少佔畫面足夠面積，孩子能用手指準確點到。

### 3.2 敘事一致性

文字若寫：

> 蜘蛛正在幫小蝙蝠把纏住的蜘蛛絲拉開。

圖片必須明確畫出：

- 蝙蝠的腳被絲纏住。
- 蜘蛛正在拉開絲。
- 蜘蛛不能畫成咬蝙蝠的姿勢。

**禁止出現「文字說 A，圖片看起來像 B」的狀況。**

### 3.3 互動安全區

產圖前先知道該頁會放哪些 hotspot：

- 一般按鈕放在哪裡？
- 哪些重要人物／物品不能被文字遮住？
- 拖曳凹槽在哪裡？

圖片構圖需為 UI 留白，不要等圖片做好才硬塞按鈕。

### 3.4 角色一致性

每本故事建立角色基準：

- 髮型
- 衣服
- 配色
- 包包／固定配件
- 陪伴動物

後續產圖必須使用同一張基準圖當 style + character reference。

---

## 4. 文字與注音標準

### 4.1 字級

平板預設：

- 標題：56–64 px
- 本文：44–48 px
- 圖片互動標籤：依畫面調整，但必須清晰可讀
- 可提供 `A− / A+` 調整本文大小

### 4.2 縮放

HTML viewport 必須允許：

```html
user-scalable=yes
maximum-scale=5
```

不可鎖死雙指縮放。

### 4.3 注音

目標是台灣兒童讀物常見的：

- 中文字在左
- ㄅㄆㄇ直排於單字右側

目前《月光洞》採用使用者提供的注音字型製作文字圖像，以避免不同平板字型支援不一致。

未來若改成 CSS `@font-face` 自帶字型，必須先確認字型授權允許網頁嵌入與散布。字型檔不應直接放進公開專案，除非授權明確允許。

---

## 5. 圖片上互動選項標準

### 5.1 一般選項

- 直接疊在圖片上。
- 半透明背景。
- 淡淡發光。
- 使用 2 秒左右呼吸動畫。
- 點擊後有輕微縮放／亮光反應。
- 不得遮住重要故事角色與道具。

### 5.2 隱藏選項

- **不發光。**
- 不做成明顯按鈕。
- 可藏在故事文字中的特定片語、地圖、符號或不起眼物件。
- 孩子沒發現仍能完成普通結局。

### 5.3 位置座標

所有 hotspot、dropzone 都應以圖片原始比例為座標基準，避免 `object-fit: cover` 裁切造成偏移。

推薦：

- 固定場景 canvas 比例。
- 圖片 `object-fit: contain` 或保持完整比例。
- hotspot 使用百分比位置。

---

## 6. 標準挑戰庫

### 6.1 `numberSequence`

用途：符文、機關、數字門鎖。

標準：

- 依序點 `1 → 2 → 3 → 4 → 5`
- 預設 5 秒限制
- **第一個數字被點下後才開始計時**
- 點錯：輕微抖動，不立即失敗
- 時間到：顯示失敗畫面
- 必須提供「重新挑戰」按鈕
- 完成後 reward 畫面不自動消失，由孩子按「繼續」

### 6.2 `jumpManual`

用途：跳躍、閃避、幫助角色脫困。

標準：

- 預設 10 次
- 顯示 10 顆空星
- 每完成一下，由孩子按一次
- 星星逐顆亮起
- 全部完成才取得道具

未來可替換成相機姿勢辨識，但平板拿在手上時不建議自動偵測跳躍。

### 6.3 `balanceTimer`

用途：平衡、走橋、拿穩物品。

目前基本版：

- 單腳站 15 秒
- 點「開始」後倒數
- 完成取得道具

未來 sensor 版：

- DeviceOrientation + DeviceMotion
- 平板必須直立／在容許角度內才計時
- 明顯晃動超過 threshold 時重新計時
- 必須有「裝置不支援 → 使用普通計時版」fallback

### 6.4 `dragDrop`

用途：鑰匙孔、拼圖、符文、道具鑲嵌。

平板標準：

- 使用 Pointer Events，不依賴 desktop HTML Drag API。
- 手指按住後產生拖曳 ghost。
- 靠近正確凹槽時凹槽發光。
- 放錯自動彈回，不顯示大紅 X。
- 放對有磁吸／縮放／光圈效果。
- **放入後圖示必須留在凹槽中，不可消失。**

---

## 7. Reward／成功畫面標準

挑戰完成後禁止：

- 只顯示 1–2 秒就自動消失。
- 文字被固定高度框裁掉。

正確做法：

1. 顯示完整 reward card。
2. 顯示取得的道具圖示。
3. 顯示「得到 XXX！」。
4. 顯示明確的「繼續」按鈕。
5. 孩子主動按繼續才進下一幕。

---

## 8. 道具與狀態標準

### 8.1 背包

右／左上角固定顯示 3 個道具位置：

- 未取得：灰色、低透明度
- 已取得：正常色＋淡發光

### 8.2 State

最少包含：

```js
{
  itemA: false,
  itemB: false,
  itemC: false,
  visitedBranchA: false,
  visitedBranchB: false,
  scene: "cover"
}
```

### 8.3 localStorage

- 自動保存進度。
- 重新整理頁面不應立即丟失進度。
- 「再玩一次」必須有明確 reset，清除本故事狀態。

---

## 9. 結局邏輯標準

推薦模式：

```text
主線可完成
   ↓
檢查道具
   ├─ 不完整 → 普通結局
   └─ 完整   → 隱藏／祕密結局
```

祕密結局：

- 不在 UI 上預告「這是祕密結局」。
- 不提供發光的「前往祕密結局」按鈕。
- 由狀態自然觸發。

普通結局必須仍有完整收尾，不把孩子當成「失敗」。

---

## 10. 建議的未來程式架構

《月光洞》為方便上傳，採 **單一 HTML 打包圖片與程式**。在多故事架構中，它放在 `stories/moon-cave/index.html`，root `index.html` 是「阿通的冒險世界」首頁。優點是部署簡單；缺點是故事檔案較大、修改不方便。本次目錄搬移不代表已抽出共用引擎。

如果之後要大量製作故事，建議改成共用引擎：

```text
interactive-story-template/
├─ index.html
├─ engine/
│  ├─ engine.js
│  ├─ challenges.js
│  ├─ sensors.js
│  └─ styles.css
├─ story/
│  ├─ story.js        # 或 story.json
│  └─ story.css       # 特殊頁面才需要
├─ assets/
│  ├─ images/
│  └─ ui/
└─ docs/
   ├─ INTERACTIVE_STORY_SOP.md
   └─ STORY_SPEC.md
```

### 引擎負責

- 場景切換
- 背包
- localStorage
- A− / A+
- hotspot 發光
- hidden link
- numberSequence
- jumpManual
- balanceTimer
- dragDrop
- future sensor API

### 新故事只修改

- 故事節點
- 文字
- 圖片
- hotspot 座標
- 道具名稱／圖示
- 分支與結局條件
- 每件道具綁哪一種 challenge

**目標：未來換故事，不重寫 engine。**

---

## 11. 新故事 story config 建議格式

未來可逐步改成資料驅動：

```js
const STORY = {
  id: "moon-cave",
  title: "月光洞的祕密",
  hero: "阿通",
  items: {
    shield: { label: "守衛盾牌", icon: "🛡️" },
    moon:   { label: "月亮石", icon: "🌙" },
    key:    { label: "古老鑰匙", icon: "🔑" }
  },
  scenes: {
    entrance: {
      image: "entrance.webp",
      text: "...",
      hotspots: [
        { id:"stone", x:5, y:80, action:"go:stone" },
        { id:"vine", x:70, y:80, action:"go:vine" }
      ]
    }
  }
};
```

之後 ChatGPT 只要依 SOP 產出新的 `story.js` + 圖片，就能套用同一套 engine。

---

## 12. GitHub Pages 部署 SOP

### 首次部署

1. GitHub 建立 Public repository。
2. repository root 放 `index.html`。
3. Settings → Pages。
4. Source：`Deploy from a branch`。
5. Branch：`main`。
6. Folder：`/(root)`。
7. Save。
8. 等待 Pages deployment 完成。

網站通常為：

```text
https://<github-user>.github.io/<repo-name>/
```

### 更新版本

多故事專案採以下流程：

1. 從最新 `main` 建立工作分支。
2. 更新對應故事的 `stories/<story-id>/index.html`；只有入口變更才修改 root `index.html`。
3. 新故事放入自己的目錄，不覆蓋既有故事；首頁以 `./stories/<story-id>/` 連入，故事使用相對路徑返回首頁。
4. 測試 project site 的 `/Moon-cave/` 前綴、資產路徑、存檔及互動，再建立 PR。
5. 使用者確認 PR／發布後才合併 `main`，等待 GitHub Pages 部署完成。
6. 驗證首頁與故事子路徑；平板若仍看到舊版，可重新整理或清除快取，勿任意清除網站存檔。

本次入口 PR 合併前，正式站仍使用原 root `index.html`。

### 檔名

網站入口固定建議：

`index.html`

不要把正式入口命名成中文長檔名。

多故事網站的 root `index.html` 為平台首頁，各故事仍以自己的 `index.html` 作為目錄入口。新故事狀態採 `adventure.<story-id>.state`，共用設定採 `adventure.settings.*`；既有故事的舊 key 需先評估 migration，不因搬移目錄直接更名。詳見 [PROJECT_HANDOFF.md](PROJECT_HANDOFF.md)。

---

## 13. 上線前 QA Checklist

### 故事

- [ ] 每一幕文字與圖片一致。
- [ ] 分支沒有死路。
- [ ] 普通結局一定能到。
- [ ] 祕密結局條件真的能完成。
- [ ] 不會因漏掉單一道具完全卡死。

### 圖片

- [ ] 主角外觀一致。
- [ ] 每幕是一張獨立對應圖。
- [ ] hotspot 沒遮住重要畫面。
- [ ] 圖片橫直比在平板正常。

### 文字

- [ ] 平板預設字夠大。
- [ ] 注音可辨識。
- [ ] A− / A+ 有效。
- [ ] 雙指縮放未被鎖定。

### 挑戰

- [ ] 成功後 reward 不會自行消失。
- [ ] 失敗有重新挑戰。
- [ ] 沒有懲罰性紅 X。
- [ ] 道具成功後真的寫入 state。

### 拖曳

- [ ] 手指可拖。
- [ ] 正確 slot 會發光。
- [ ] 圖示鑲嵌後仍然存在。
- [ ] 凹槽位置與原圖一致。

### 部署

- [ ] HTTPS GitHub Pages 可正常開。
- [ ] Android 平板測試。
- [ ] iPad 若有則測試。
- [ ] 重新整理後狀態符合預期。
- [ ] `再玩一次` 能真正清空狀態。

---

## 14. 版本與開發紀錄規則

推薦版本號：

- `v1`：第一個可玩的完整版本
- `v2`：修 UI／功能問題
- `v3`：加入挑戰時間限制等新互動
- `v4`：若加入 sensor 體感，視為功能升級

每次更新在 `MOON_CAVE_DEVLOG.md` 或該故事自己的 DEVLOG 中記錄：

```md
## v4 - YYYY-MM-DD
### Added
- 單腳平衡支援 DeviceOrientation。

### Changed
- 盾牌數字挑戰由 7 秒改為 5 秒。

### Fixed
- 道具放入石門後圖示消失。
```

---

## 15. 未來體感擴充原則

可重用 sensor challenge：

- `balanceSensor`：保持平板穩定／直立。
- `tiltMaze`：傾斜平板控制球。
- `shake`：搖晃平板指定次數。
- `orientationFind`：轉動平板找方向。

跳躍不優先使用平板 accelerometer，因為孩子拿平板跳躍有掉落與跌倒風險。若未來要自動判斷跳躍，優先評估「平板放置固定位置 + 前鏡頭本機姿勢辨識」。

每個 sensor challenge 必須有非 sensor fallback，避免舊設備或權限被拒絕時卡關。

---

## 16. 新故事開發的標準順序

固定依序進行：

1. **故事概念**：主題、角色、教育點。
2. **完整分支文字**：先把所有 scene + 選項寫完。
3. **狀態／道具檢查**：確認祕密結局可達成。
4. **圖片需求表**：逐幕定義必要元素與 UI 安全區。
5. **先產角色／世界觀基準圖。**
6. **逐幕產圖。**
7. **確認圖片與故事一致。**
8. **套入共用網頁引擎。**
9. **設定 hotspot／dropzone 座標。**
10. **平板實機測試。**
11. **修 UI／字級／挑戰難度。**
12. **GitHub Pages 發布。**
13. **更新 DEVLOG。**

除非前一步已確認，不要直接跳到後面的程式製作。

---

## 17. 新故事啟動用簡短指令

未來開新故事時，可直接提供以下內容給協作者或 ChatGPT：

> 請依 `INTERACTIVE_STORY_SOP.md` 開發新的平板互動兒童冒險繪本。先不要寫程式。第一階段只完成：故事概念、完整分支、三件以內的道具、普通／祕密雙結局，以及每幕的圖片必要元素。等我確認後才進入產圖與開發。

這一句配合本 SOP，即可避免每次重新溝通整套規則。
