# 冒險入口調整驗證紀錄

- 日期：2026-09-24
- 分支：`feature/adventure-hub`
- 狀態：待使用者確認 PR；尚未合併／發布。
- 正式故事：仍為《月光洞的祕密》v3。
- 比對基準：commit `50841bd009b1a14e00f673b23fa5e5b7828dfb66` 的 root `index.html`。

## 已完成的驗證

測試環境為 Windows 上的 Playwright 1.59.0／Chromium，搭配觸控事件模擬與 localhost HTTP 伺服器。伺服器保留 GitHub Pages 的 `/Moon-cave/` 路徑前綴；倒數由測試時鐘加速。

| 驗收項目 | 結果與範圍 |
| --- | --- |
| 保存原 v3 | Git 歷史保留完整原檔；blob `2071c1f9b662637e7db6708217d8d3c20c8cec5d`。 |
| 搬移完整性 | 移動版移除新增的返回導覽後，與原檔全文一致（正規化換行）；原 JS、CSS、所有故事圖片及注音資料均未變更。 |
| 首頁圖片 | 從原 `IMAGES.cover` 抽出 WebP，位元內容完全相同，未重新產圖或裁切。 |
| 路徑與資產 | `/Moon-cave/` 顯示首頁；故事卡及開始按鈕前往 `/Moon-cave/stories/moon-cave/`；原故事無外部相對路徑依賴。 |
| 返回首頁 | 故事頁底連結回到 `/Moon-cave/`，不遮擋故事、不重置存檔。 |
| 平板／手機尺寸 | 820×1180、768×1024、1024×1366 直向，1180×820 橫向，以及 390×844；無橫向溢出，按鈕高至少 56 px，圖片載入成功。 |
| 字級與縮放設定 | A−／A+ 有效，重新整理保留；viewport 仍允許縮放至 5 倍。實際雙指手勢待實機測。 |
| 守衛盾牌 | 未點數字不倒數；第一次點擊開始 5 秒；錯誤不立即失敗；超時可重試；依序完成取得盾牌。 |
| 月亮石 | 前 9 次不取得道具，逐顆亮星；第 10 次取得月亮石。 |
| 古老鑰匙 | 按開始前不倒數；14 秒時尚未取得，滿 15 秒取得。 |
| 成功卡片 | 三個挑戰成功後額外等待 5 秒仍停留；按「繼續」才進入下一幕。 |
| 隱藏探索 | 由地圖回到未走過的路；隱藏入口沒有動畫或發光陰影。 |
| 拖曳 | 使用瀏覽器觸控事件；放錯不填入，靠近凹槽有提示；放對保留圖示及發光樣式；原凹槽座標不變。 |
| 雙結局 | 完整三件道具可達祕密結局；只帶鑰匙也可完成普通結局；完成後可再玩一次。結局觸控模擬限制見下節。 |
| 舊存檔續玩 | 先在原根入口建立 v3 存檔，再切換新首頁與子路徑；場景、分支與字級值完全保留。 |
| 重新整理 | 場景、道具與字級保留；石門凹槽需重放，與原 v3 一致。 |
| 比對結果 | 同一組操作在原版及移動版得到相同最終存檔與相同結局點擊次數。故事流程未出現 JavaScript 錯誤或 HTTP 資產錯誤。 |

重跑方式：

```sh
python tests/adventure_hub_test.py --output ../hub-review
```

6 組檢查通過：保存完整性、響應式導覽、舊存檔、原 v3 流程、搬移版流程、原版／搬移版結果一致性。測試會輸出 JSON 與截圖，詳細依賴見 [tests/README.md](tests/README.md)。

## 首頁注音修正的追加驗證

依使用者回饋，首頁全部中文及新增返回導覽改為指定 `BpmfGenRyuMin-H.ttf` 渲染的透明 PNG，合計 10 張；共用 SOP／HANDOFF 同時規定後續所有故事使用此字型。

- 使用 Pillow 12.2.0，字型直接讀自使用者提供的本機檔案；沒有複製或提交字型。
- 文字以 192 px 渲染並保留透明邊界，產圖時逐張檢查非空、未碰邊；已目視檢查右側注音與小尺寸排版。
- 再次執行 `python tests/adventure_hub_test.py --output ../hub-review/font-update --only hub`，保存完整性、5 種尺寸導覽、舊存檔續玩共 3 組通過。
- 首頁 9 張文字圖與返回連結均正常載入；標題與按鈕保留可存取名稱，瀏覽器未請求任何 TTF／OTF／WOFF 字型檔。
- 本次仍只改首頁與獨立返回導覽，原 v3 故事內容／程式比對不變，因此沿用上面的三挑戰、拖曳及雙結局回歸結果。

## 已知限制與待確認項目

### 拖曳後重新整理的觸控模擬

Chromium 自動化中，使用原生模擬觸控拖曳、進入結局並重新整理後，第一次點「完成冒險」會產生 touch／pointer 事件，但沒有 click；第二次可完成。原 v3 與搬移版的普通、祕密結局都需要 2 次，結果相同。單獨載入祕密結局的獨立檢查可一次完成。

測試對此具體情境記錄點擊次數並比對兩版，未用全域重試掩蓋失敗，也未修改故事程式。目前不能據此斷定實體裝置有同樣問題；請在 Android 平板／iPad 確認「拖曳 → 結局 → 重新整理 → 完成冒險」。

### 實機與正式部署

- 尚未做 Android 平板／iPad 實機測試；觸控模擬不能代替手指拖曳手感與雙指縮放。
- `main` 未變更，正式 GitHub Pages 尚未切換。新路徑已在本機按相同前綴驗證，未宣稱線上已發布。
- 使用者確認 PR 後才可合併。部署完成需再確認 `/Moon-cave/`、`/Moon-cave/stories/moon-cave/`、返回連結、圖片載入及既有裝置存檔。
- 故事 key 保持 `moonCaveState`／`moonCaveTextScale`；namespace migration 只提出建議，未執行。
- 未建立 engine、未開始 Story 02，未加入字型檔或公開未授權字型。
